export type History = {
  date: string;
  action_type: string;
  description: string;
  technical_specification: string;
  image: string;
};

export type Room = {
  name: string;
  lamp: number;
  socket: number;
};
